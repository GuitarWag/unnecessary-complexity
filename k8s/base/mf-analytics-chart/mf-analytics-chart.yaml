apiVersion: v1
kind: Service
metadata:
  name: mf-analytics-chart
  namespace: url-shortener
  labels: { app: mf-analytics-chart }
spec:
  selector: { app: mf-analytics-chart }
  ports:
    - { name: http, port: 80, targetPort: 80 }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mf-analytics-chart
  namespace: url-shortener
  labels: { app: mf-analytics-chart }
spec:
  replicas: 1
  selector:
    matchLabels: { app: mf-analytics-chart }
  template:
    metadata:
      labels: { app: mf-analytics-chart }
    spec:
      containers:
        - name: nginx
          image: url-shortener/mf-analytics-chart:dev
          imagePullPolicy: IfNotPresent
          ports:
            - { containerPort: 80, name: http }
          readinessProbe:
            httpGet: { path: /healthz, port: 80 }
            initialDelaySeconds: 2
