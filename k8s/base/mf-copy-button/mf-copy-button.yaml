apiVersion: v1
kind: Service
metadata:
  name: mf-copy-button
  namespace: url-shortener
  labels: { app: mf-copy-button }
spec:
  selector: { app: mf-copy-button }
  ports:
    - { name: http, port: 80, targetPort: 80 }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mf-copy-button
  namespace: url-shortener
  labels: { app: mf-copy-button }
spec:
  replicas: 1
  selector:
    matchLabels: { app: mf-copy-button }
  template:
    metadata:
      labels: { app: mf-copy-button }
    spec:
      containers:
        - name: nginx
          image: url-shortener/mf-copy-button:dev
          imagePullPolicy: IfNotPresent
          ports:
            - { containerPort: 80, name: http }
          readinessProbe:
            httpGet: { path: /healthz, port: 80 }
            initialDelaySeconds: 2
