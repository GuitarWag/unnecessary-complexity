apiVersion: v1
kind: Service
metadata:
  name: mf-url-input
  namespace: url-shortener
  labels: { app: mf-url-input }
spec:
  selector: { app: mf-url-input }
  ports:
    - { name: http, port: 80, targetPort: 80 }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mf-url-input
  namespace: url-shortener
  labels: { app: mf-url-input }
spec:
  replicas: 1
  selector:
    matchLabels: { app: mf-url-input }
  template:
    metadata:
      labels: { app: mf-url-input }
    spec:
      containers:
        - name: nginx
          image: url-shortener/mf-url-input:dev
          imagePullPolicy: IfNotPresent
          ports:
            - { containerPort: 80, name: http }
          readinessProbe:
            httpGet: { path: /healthz, port: 80 }
            initialDelaySeconds: 2
