apiVersion: v1
kind: Service
metadata:
  name: mf-url-list
  namespace: url-shortener
  labels: { app: mf-url-list }
spec:
  selector: { app: mf-url-list }
  ports:
    - { name: http, port: 80, targetPort: 80 }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mf-url-list
  namespace: url-shortener
  labels: { app: mf-url-list }
spec:
  replicas: 1
  selector:
    matchLabels: { app: mf-url-list }
  template:
    metadata:
      labels: { app: mf-url-list }
    spec:
      containers:
        - name: nginx
          image: url-shortener/mf-url-list:dev
          imagePullPolicy: IfNotPresent
          ports:
            - { containerPort: 80, name: http }
          readinessProbe:
            httpGet: { path: /healthz, port: 80 }
            initialDelaySeconds: 2
