// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: shortener/v1/shortener.proto

package shortenerv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/yld/url-shortener/services/proto/gen/shortener/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// ShortenerServiceName is the fully-qualified name of the ShortenerService service.
	ShortenerServiceName = "shortener.v1.ShortenerService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// ShortenerServiceShortenProcedure is the fully-qualified name of the ShortenerService's Shorten
	// RPC.
	ShortenerServiceShortenProcedure = "/shortener.v1.ShortenerService/Shorten"
	// ShortenerServiceGetProcedure is the fully-qualified name of the ShortenerService's Get RPC.
	ShortenerServiceGetProcedure = "/shortener.v1.ShortenerService/Get"
)

// ShortenerServiceClient is a client for the shortener.v1.ShortenerService service.
type ShortenerServiceClient interface {
	// Shorten creates a short code for a long URL. Idempotent on long_url:
	// submitting the same long_url twice returns the existing code.
	Shorten(context.Context, *connect.Request[v1.ShortenRequest]) (*connect.Response[v1.ShortenResponse], error)
	// Get returns the mapping for a code.
	Get(context.Context, *connect.Request[v1.GetRequest]) (*connect.Response[v1.GetResponse], error)
}

// NewShortenerServiceClient constructs a client for the shortener.v1.ShortenerService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewShortenerServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) ShortenerServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	shortenerServiceMethods := v1.File_shortener_v1_shortener_proto.Services().ByName("ShortenerService").Methods()
	return &shortenerServiceClient{
		shorten: connect.NewClient[v1.ShortenRequest, v1.ShortenResponse](
			httpClient,
			baseURL+ShortenerServiceShortenProcedure,
			connect.WithSchema(shortenerServiceMethods.ByName("Shorten")),
			connect.WithClientOptions(opts...),
		),
		get: connect.NewClient[v1.GetRequest, v1.GetResponse](
			httpClient,
			baseURL+ShortenerServiceGetProcedure,
			connect.WithSchema(shortenerServiceMethods.ByName("Get")),
			connect.WithClientOptions(opts...),
		),
	}
}

// shortenerServiceClient implements ShortenerServiceClient.
type shortenerServiceClient struct {
	shorten *connect.Client[v1.ShortenRequest, v1.ShortenResponse]
	get     *connect.Client[v1.GetRequest, v1.GetResponse]
}

// Shorten calls shortener.v1.ShortenerService.Shorten.
func (c *shortenerServiceClient) Shorten(ctx context.Context, req *connect.Request[v1.ShortenRequest]) (*connect.Response[v1.ShortenResponse], error) {
	return c.shorten.CallUnary(ctx, req)
}

// Get calls shortener.v1.ShortenerService.Get.
func (c *shortenerServiceClient) Get(ctx context.Context, req *connect.Request[v1.GetRequest]) (*connect.Response[v1.GetResponse], error) {
	return c.get.CallUnary(ctx, req)
}

// ShortenerServiceHandler is an implementation of the shortener.v1.ShortenerService service.
type ShortenerServiceHandler interface {
	// Shorten creates a short code for a long URL. Idempotent on long_url:
	// submitting the same long_url twice returns the existing code.
	Shorten(context.Context, *connect.Request[v1.ShortenRequest]) (*connect.Response[v1.ShortenResponse], error)
	// Get returns the mapping for a code.
	Get(context.Context, *connect.Request[v1.GetRequest]) (*connect.Response[v1.GetResponse], error)
}

// NewShortenerServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewShortenerServiceHandler(svc ShortenerServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	shortenerServiceMethods := v1.File_shortener_v1_shortener_proto.Services().ByName("ShortenerService").Methods()
	shortenerServiceShortenHandler := connect.NewUnaryHandler(
		ShortenerServiceShortenProcedure,
		svc.Shorten,
		connect.WithSchema(shortenerServiceMethods.ByName("Shorten")),
		connect.WithHandlerOptions(opts...),
	)
	shortenerServiceGetHandler := connect.NewUnaryHandler(
		ShortenerServiceGetProcedure,
		svc.Get,
		connect.WithSchema(shortenerServiceMethods.ByName("Get")),
		connect.WithHandlerOptions(opts...),
	)
	return "/shortener.v1.ShortenerService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case ShortenerServiceShortenProcedure:
			shortenerServiceShortenHandler.ServeHTTP(w, r)
		case ShortenerServiceGetProcedure:
			shortenerServiceGetHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedShortenerServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedShortenerServiceHandler struct{}

func (UnimplementedShortenerServiceHandler) Shorten(context.Context, *connect.Request[v1.ShortenRequest]) (*connect.Response[v1.ShortenResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("shortener.v1.ShortenerService.Shorten is not implemented"))
}

func (UnimplementedShortenerServiceHandler) Get(context.Context, *connect.Request[v1.GetRequest]) (*connect.Response[v1.GetResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("shortener.v1.ShortenerService.Get is not implemented"))
}
